apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seo-discovery-state-pvc
  namespace: planetary-pulse
  labels:
    app: seo-discovery-engine
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: standard-rwo
